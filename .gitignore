# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Environment / secrets ---
.env

# --- Data (real GTFS feeds go here; the synthetic seed feed IS committed) ---
# anchored to the repo root: feature `data/` layers in the Flutter app must
# NOT be ignored (unanchored `data/` once swallowed mobile/lib/features/*/data)
/data/

# --- Flutter / Dart ---
mobile/.dart_tool/
mobile/build/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/.packages
mobile/ios/Pods/
mobile/ios/.symlinks/
mobile/android/.gradle/
mobile/android/local.properties
# generated code is committed (see README) — do NOT ignore *.g.dart / *.freezed.dart

# --- IDE ---
.idea/
.vscode/
*.iml
.DS_Store
PLAN.md
