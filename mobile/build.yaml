# json_serializable: API JSON uses snake_case keys; models use lowerCamelCase.
targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
